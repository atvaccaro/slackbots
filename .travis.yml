language: python
python:
    - "2.7"
sudo: required
before_install:
    - apt-get install enchant
    - apt-get install aspell-en enchant-aspell
install:
    - pip install -r requirements.txt
script: nosetests
